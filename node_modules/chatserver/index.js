var ChatServer=require('./lib/ChatServer');
var ChatServerConfig=require('./lib/ChatServerConfig');

exports.getInstance=function(config) {
	return ChatServer.createServer({
		config: new ChatServerConfig(config)
	});
};
