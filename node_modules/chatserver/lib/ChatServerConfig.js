var Config = require('./constants/config');
var Underscore= require('underscore');
var Args=require('yargs');

module.exports = ChatServerConfig;

function ChatServerConfig(options) {
	var x=Underscore.clone(Config.defaults);
	Config.apply_options(options || {},x);
	Underscore.extend(this,x);
}